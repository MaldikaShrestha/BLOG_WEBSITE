Django==5.0.6                 # or your blog's Django version
gunicorn==21.2.0
psycopg2-binary==2.9.9        # PostgreSQL driver
python-dotenv==1.2.1          # For environment variables
whitenoise==6.5.0             # Serve static files in production
requests==2.32.5              # Optional, if your blog uses it
pillow==11.2.1                # Optional, for images